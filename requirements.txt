# Smart Image Recognition System — Python Dependencies
# =====================================================
# Install with: pip install -r requirements.txt

# ── Web Framework ─────────────────────────────────
Flask==3.0.3
Werkzeug==3.0.3

# ── Machine Learning ──────────────────────────────
tensorflow==2.17.0          # Includes Keras 3; use tensorflow-cpu for non-GPU servers
# tensorflow-cpu==2.17.0    # Uncomment for CPU-only deployment (smaller image)

# ── Computer Vision ───────────────────────────────
opencv-python-headless==4.10.0.84   # headless = no GUI libs, ideal for servers

# ── Numerical Computing (pulled by TF, pinned for stability) ──
numpy==1.26.4

# ── Production WSGI Server ────────────────────────
gunicorn==22.0.0

# ── Utilities ─────────────────────────────────────
python-dotenv==1.0.1        # Load .env for local development